spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/thenakedbookstore
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  properties:
    hibernate:
      format_sql : true
    database: mysql
    database-platform: rg.hibernate.dialect.MySQL8Dialect
